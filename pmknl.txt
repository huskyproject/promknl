
                          ProMkNL - documentation

Not finished yet.

Sample pmknl.cfg etc. are included.

It's similar to MkNL, so consult the documentation of MkNL first.

Author: Sascha Silbe, 2:2435/424, Sascha@silbe.org

environment variables:
PMKNL           path where PMKNL.CFG can be found (if nothing is found there,
                the current directory is used)
MSGID           path where msgid.dat can be found (if nothing is found there,
                the master directory from the config is used)

